services:
  volleymindset:
    image: node:20-alpine
    container_name: volleymindset
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "127.0.0.1:8809:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    command: node server.js
